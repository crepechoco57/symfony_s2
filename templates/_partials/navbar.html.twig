  <header>
    <div class="container">
        <h1>Your Site Title</h1>
    
    </div>
    <nav>
        <ul>
            <li><a href="{{ path('app_produits') }}">Afficher Produits</a></li>
            <li><a href="{{ path('app_ajouter_produits') }}">Ajouter Produits</a></li>
            <li><a href="{{ path('app_register') }}">S'enregistrer</a></li>
            <li><a href="{{ path('app_login') }}">Login</a></li>
            {% if app.user %}
        <a href="{{ path('app_logout') }}" class ="btn btndanger">Deconexion</a>
            {% else %}
            <a href="{{ path('app_login') }}"></a>
            {% endif %}
            {# <li><a href="{{ path('app_logout') }}">Logout</a></li> #}
        </ul>
    </nav>
  </header>